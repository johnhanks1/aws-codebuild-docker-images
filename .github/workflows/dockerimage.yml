name: Docker Image CI

on: [pull_request, push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: 
        VERSION=$(date +%s)
        docker build . --file al2/x86_64/standard/1.0/Dockerfile --tag al2:${VERISON}
